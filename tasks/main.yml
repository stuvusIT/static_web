---
- name: Clone websites
  git:
    accept_hostkey: yes
    clone: yes
    update: yes
    dest: "{{ item.to }}"
    force: yes
    key_file: "{{ item.keyfile | default(None) }}"
    repo: "{{ item.src }}"
  with_items: "{{ static_web_folder_to_copy }}"

